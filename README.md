There are a number of ways to run the project
1. If you have maven installed, from the root sandbox directory:
    mvn spring-boot:run 

2. Using java 8, from the root sandbox directory:
   java -jar target/NotesRestApi-0.0.1-SNAPSHOT.jar
